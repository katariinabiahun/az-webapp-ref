servicebus:
  namespace:
    srvbusnamespaceicelab:
      sku: Premium #Private access is only available on Premium namespaces.
      capacity: 1 #When sku is Premium, capacity can be 1, 2, 4, 8 or 16
  queue:
    tfex_servicebus_queue:
      enable_partitioning: false
  auth_rule:
    examplerule:
      listen: true
      send: true
      manage: true #if `manage` is set both `listen` and `send` must be set to true too
  network_rule_set:
    set1:
      default_action: Deny
      public_network_access_enabled: false
      ignore_missing_vnet_service_endpoint: false
